---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Partition then re-parse to accept flags in any position](cli.md) (search: "## Pattern: Partition then re-parse to accept flags in any position") - Go's `flag.FlagSet.Parse` stops at the first operand, so every gruff-go subcommand that took…
- [User-facing errors name the bad value, the expected value, and the exact remediation command](error-handling.md) (search: "## Pattern: User-facing errors name the bad value, the expected value, and the exact remediation command") - Any error that rejects user input — config field, baseline file, CLI flag value, schema version,…
- [Keep rule metadata out of per-unit dispatch](performance.md) (search: "## Pattern: Keep rule metadata out of per-unit dispatch") - `scripts/test-performance.sh --sweep` showed rule-set timings dominated by fixed per-scan dispatch…
- [Use AST comments for Go suppression directives](rules.md) (search: "## Pattern: Use AST comments for Go suppression directives") - While adding `docs.suppression-without-rationale`, the first line-scan implementation treated…
- [Calibrate a metric threshold by sweeping the distribution, then cross-check nesting](rules.md) (search: "## Pattern: Calibrate a metric threshold by sweeping the distribution, then cross-check nesting") - Deciding whether `complexity.npath`'s default (1024) was right needed the *actual distribution* of…
- [Ship broad parser-only dead-code checks as opt-in candidates first](rules.md) (search: "## Pattern: Ship broad parser-only dead-code checks as opt-in candidates first") - M28 expanded `dead-code.unused-private-function` into a shared package reference index and added…
- [Test static-shape rules with paired flag and no-flag probes](rules.md) (search: "## Pattern: Test static-shape rules with paired flag and no-flag probes") - Adding `test-quality.static-analysis-redundant-test` required proving both sides of a narrow…
- [Validate a precision/rule change against the corpus before shipping](rules.md) (search: "## Pattern: Validate a precision/rule change against the corpus before shipping") - A default-rule precision change (a new skip, a broadened or narrowed match) can fix one false…
- [Anchor marker vocabulary to introduced physical lines](rules.md) (search: "## Pattern: Anchor marker vocabulary to introduced physical lines") - `test-quality.skipped-test` used case-insensitive substring search over literal `t.Skip` arguments.
- [Map cross-port defects to native rule ownership before porting](rules.md) (search: "## Pattern: Map cross-port defects to native rule ownership before porting") - A gruff-ts false-positive pass named process execution, loop-in-test labelling, and nested…
- [Re-derive numeric claims from the live registry before writing architecture](setup.md) (search: "## Pattern: Re-derive numeric claims from the live registry before writing architecture") - `.goat-flow/architecture.md` and `.goat-flow/code-map.md` carried a "29-rule default-enabled…
- [Collapse lockstep-dependency defaults into one helper that all sites consume](setup.md) (search: "## Pattern: Collapse lockstep-dependency defaults into one helper that all sites consume") - ADR-009 (3-bucket severity migration) left four scattered fallback constants for the per-command…
