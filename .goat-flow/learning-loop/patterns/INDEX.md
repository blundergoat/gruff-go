---
category: index
bucket: patterns
generated: true
---

# Patterns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one pattern entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [User-facing errors name the bad value, the expected value, and the exact remediation command](error-handling.md) (search: "## Pattern: User-facing errors name the bad value, the expected value, and the exact remediation command") - Any error that rejects user input — config field, baseline file, CLI flag value, schema version, severity bucket, threshold sentinel, YAML decode failure.
- [Keep rule metadata out of per-unit dispatch](performance.md) (search: "## Pattern: Keep rule metadata out of per-unit dispatch") - `scripts/test-performance.sh --sweep` showed rule-set timings dominated by fixed per-scan dispatch overhead rather than one expensive rule.
- [Use AST comments for Go suppression directives](rules.md) (search: "## Pattern: Use AST comments for Go suppression directives") - While adding `docs.suppression-without-rationale`, the first line-scan implementation treated `//nolint` and `#nosec` text inside raw string fixtures and explanatory doc comments as live suppressions.
- [Calibrate a metric threshold by sweeping the distribution, then cross-check nesting](rules.md) (search: "## Pattern: Calibrate a metric threshold by sweeping the distribution, then cross-check nesting") - Deciding whether `complexity.npath`'s default (1024) was right needed the *actual distribution* of the metric across real code, not a guess.
- [Ship broad parser-only dead-code checks as opt-in candidates first](rules.md) (search: "## Pattern: Ship broad parser-only dead-code checks as opt-in candidates first") - M28 expanded `dead-code.unused-private-function` into a shared package reference index and added unused private type, var, and const checks.
- [Test static-shape rules with paired flag and no-flag probes](rules.md) (search: "## Pattern: Test static-shape rules with paired flag and no-flag probes") - Adding `test-quality.static-analysis-redundant-test` required proving both sides of a narrow parser-only contract: same-package reflection assertions that restate source declarations should flag,…
- [Validate a precision/rule change against the corpus before shipping](rules.md) (search: "## Pattern: Validate a precision/rule change against the corpus before shipping") - A default-rule precision change (a new skip, a broadened or narrowed match) can fix one false positive while creating others, and unit fixtures rarely cover real-world shapes.
- [Re-derive numeric claims from the live registry before writing architecture](setup.md) (search: "## Pattern: Re-derive numeric claims from the live registry before writing architecture") - `.goat-flow/architecture.md` and `.goat-flow/code-map.md` carried a "29-rule default-enabled registry" claim across four lines while `go run ./cmd/gruff-go list-rules --format json` reported 30 rules.
- [Collapse lockstep-dependency defaults into one helper that all sites consume](setup.md) (search: "## Pattern: Collapse lockstep-dependency defaults into one helper that all sites consume") - ADR-009 (3-bucket severity migration) left four scattered fallback constants for the per-command `--min-severity` default - `internal/cli/cli.go`, `internal/cli/summary.go`, `internal/cli/report.go`,…
