---
category: index
bucket: lessons
generated: true
---

# Lessons Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one lesson entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Do not backtick nonexistent illustrative paths](setup.md) (search: "## Lesson: Do not backtick nonexistent illustrative paths") - The 2026-05-13 harness audit failed `doc-paths-resolve` after `.goat-flow/code-map.md` used an absent source directory as a backticked example.
- [Harness advisory failures still block zero-failure setup](setup.md) (search: "## Lesson: Harness advisory failures still block zero-failure setup") - The base audit passed, but the 2026-05-13 harness audit failed `commit-guidance` because `.github/git-commit-instructions.md` was missing.
- [reproduce a coding-agent PR-review finding before fixing it](verification.md) (search: "## Lesson: reproduce a coding-agent PR-review finding before fixing it") - Coding-agent reviewers (Codex, Cursor, Copilot, CodeRabbit) describe behaviour they infer from a diff, and some of that inferred behaviour is intended, not a bug.
- [Milestone `Status: planned` can lag the code by entire milestones — verify against the codebase before executing](workflow.md) (search: "## Lesson: Milestone `Status: planned` can lag the code by entire milestones — verify against the codebase before executing") - A 0.1.2 execution turn started by reading `tasks/0.1.2/M01-failthreshold-type-and-none-sentinel.md`, which said `Status: planned` and had every task checkbox unticked.
- [When a milestone says `Status: complete` but every inner checkbox is `[ ]`, the contradiction is the headline, not a footnote](workflow.md) (search: "## Lesson: When a milestone says `Status: complete` but every inner checkbox is `[ ]`, the contradiction is the headline, not a footnote") - A 0.1.2 close-out turn started with the request "execute these: .goat-flow/tasks/0.1.2".
- [A vocabulary migration is not complete until `docs/` is swept](workflow.md) (search: "## Lesson: A vocabulary migration is not complete until `docs/` is swept") - PR #3 (`v0.1.2`) executes ADR-009: a hard-break migration of the severity vocabulary from 5 buckets (`critical/high/medium/low/info`, plus the `notice`/`warn` aliases) to 3 (`advisory/warning/error`).
- [After an enum rename, sweep test failure messages for the old name](workflow.md) (search: "## Lesson: After an enum rename, sweep test failure messages for the old name") - ADR-009 renamed severity constants (`SeverityMedium` → `SeverityWarning`, `SeverityLow` → `SeverityAdvisory`, etc.) across the rule package and its tests.
- [Never name source files after milestone/sprint IDs - file names survive every prose grep](workflow.md) (search: "## Lesson: Never name source files after milestone/sprint IDs - file names survive every prose grep") - `internal/rule/` carried ten files named after retired internal milestone IDs: `security_m08.go`, `security_m08_test.go`, `security_m37_test.go`, `security_m38_test.go`, `maintainability_m08.go`,…
- [Check git history before treating dogfood noise as a rubric gap](workflow.md) (search: "## Lesson: Check git history before treating dogfood noise as a rubric gap") - The user's preflight check reported 25 sensitive-data findings in `internal/rule/sensitive_test.go` and `internal/report/sensitive_redaction_test.go`.
- [Check scanner config before agent-skill rubrics](workflow.md) (search: "## Lesson: Check scanner config before agent-skill rubrics") - When asked to list security rubrics, the agent searched for `rubric` and returned the GOAT critique/security-assessment rubric from `.agents/skills/` instead of first checking the project scanner…
- [Place calibration helpers before dogfood, not after](workflow.md) (search: "## Lesson: Place calibration helpers before dogfood, not after") - While fixing false positives in `internal/rule/builtin.go`, the agent added helper code directly to the already-large builtin rule file.
- [Temporary in-tree verification harnesses still participate in dogfood](workflow.md) (search: "## Lesson: Temporary in-tree verification harnesses still participate in dogfood") - While independently checking `test-quality.static-analysis-redundant-test`, an untracked temporary test file was left under `internal/rule/` with the explicit header `TEMPORARY…
