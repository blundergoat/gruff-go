---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- ["dogfood must be grade A" tempts you to mute a tripping rule with a per-repo threshold override](calibration.md) (search: '## Footgun: "dogfood must be grade A" tempts you to mute a tripping rule with a per-repo threshold override') - The project invariant is that `go run ./cmd/gruff-go analyse .` returns grade A on `main`.
- [the scan-test-repos corpus is gitignored, so a root-relative scan finds nothing](calibration.md) (search: "## Footgun: the scan-test-repos corpus is gitignored, so a root-relative scan finds nothing") - The precision corpus lives under `.goat-flow/scratchpad/scan-test-repos/`, which is gitignored in…
- [finding filters do not isolate rule execution or scoring](calibration.md) (search: "## Footgun: finding filters do not isolate rule execution or scoring") - A probe run with `--include-rules naming.acronym-case` can render an empty `findings` array while…
- [Go's `flag` package stops parsing at the first operand](cli.md) (search: "## Footgun: Go's `flag` package stops parsing at the first operand") - hallucination-risk: high (the stdlib behaviour produces no error and no warning — a flag placed…
- [`go run` does not propagate the program's exit code](cli.md) (search: "## Footgun: `go run` does not propagate the program's exit code") - hallucination-risk: medium (the number looks like a real exit code and is plausible — `1` where the…
- [the agent hook re-implements `internal/diff` changed-region logic in bash — the two drift](hooks.md) (search: "## Footgun: the agent hook re-implements `internal/diff` changed-region logic in bash — the two drift") - hallucination-risk: high (both layers "do changed-region scoping", so a fix to the Go core reads as…
- [the hook is advisory exit 0 with in-band skip/ignore reporting — do not make it exit 2](hooks.md) (search: "## Footgun: the hook is advisory exit 0 with in-band skip/ignore reporting — do not make it exit 2") - `analyse` exits 2 when every explicit input is skipped before parsing: `ReportAllSkippedInputs`…
- [protected-path literals inside data filters can trigger the secret-file guard](hooks.md) (search: "## Footgun: protected-path literals inside data filters can trigger the secret-file guard") - hallucination-risk: medium
- [a local fix inside a managed goat-flow hook is reverted by the next install, and the drift signal is inverted](hooks.md) (search: "## Footgun: a local fix inside a managed goat-flow hook is reverted by the next install, and the drift signal is inverted") - `.goat-flow/hooks/post-turn-safety.sh` is a managed file: `goat-flow install` restores it from…
- [Workflow action ref safety is shape-based, not tag-aware](security-rules.md) (search: "## Footgun: Workflow action ref safety is shape-based, not tag-aware") - `security.github-actions-unpinned-action` is a parser-only text rule.
- [Template XSS must classify Execute receivers, not file imports](security-rules.md) (search: "## Footgun: Template XSS must classify Execute receivers, not file imports") - Importing `html/template` in a file does not make `text/template` auto-escaped.
- [secret-pattern precision guards are Go-only; config files and test fixtures both bite](security-rules.md) (search: "## Footgun: secret-pattern precision guards are Go-only; config files and test fixtures both bite") - The generic `sensitive-data.secret-pattern` rule (`internal/rule/builtin.go`, search: `func (r…
- [an early return added before `url.Parse` silently widens which credentials are reported](security-rules.md) (search: "## Footgun: an early return added before `url.Parse` silently widens which credentials are reported") - `splitConnectionURL` (`internal/rule/sensitive.go`, search: `func splitConnectionURL`) returns a…
- [`ast.Inspect` flattens control flow, so a nested `break` reads as an escape from the outer loop](security-rules.md) (search: "## Footgun: `ast.Inspect` flattens control flow, so a nested `break` reads as an escape from the outer loop") - `loopTrimsOneLeadingSlash` (`internal/rule/security_request_url_constraints.go`, search: `func…
- [an invalidation check must match the property the guard proved](security-rules.md) (search: "## Footgun: an invalidation check must match the property the guard proved") - The request-URL rules expire a guard once the guarded value can change before the sink, via…
- [a statement-order dominance proof is undone by goto, closures, and address-of](security-rules.md) (search: "## Footgun: a statement-order dominance proof is undone by goto, closures, and address-of") - Suppressing a request-taint finding because a later write replaced the value needs the write to…
- [a normalisation proof must cover every spelling the sink accepts](security-rules.md) (search: "## Footgun: a normalisation proof must cover every spelling the sink accepts") - hallucination-risk: medium (the loop *looks* like complete same-origin normalisation, and it is the…
- [`allowlists.acceptedAbbreviations` has different rule-consumer semantics than sibling gruff ports](setup.md) (search: "## Footgun: `allowlists.acceptedAbbreviations` has different rule-consumer semantics than sibling gruff ports") - hallucination-risk: high (the shared key name across gruff-go / gruff-rs / gruff-ts / gruff-py /…
- [`allowlists.secretPreviews` gates the preview field only - it does not suppress sensitive-data findings](setup.md) (search: "## Footgun: `allowlists.secretPreviews` gates the preview field only - it does not suppress sensitive-data findings") - hallucination-risk: medium (the field name and sibling configuration invite an incorrect mental…
- [`gruff-go init --reset` wipes hand-tuned `.gruff-go.yaml` policy](setup.md) (search: "## Footgun: `gruff-go init --reset` wipes hand-tuned `.gruff-go.yaml` policy") - hallucination-risk: low (this is a behaviour to remember, not a fact to fabricate)
- [`npm test` exists but is a failing placeholder](setup.md) (search: "## Footgun: `npm test` exists but is a failing placeholder") - hallucination-risk: high
- [Scanner CLI exists, but published operational integration is still narrow](setup.md) (search: "## Footgun: Scanner CLI exists, but published operational integration is still narrow") - hallucination-risk: high
- [release docs lag the version literals; committed docs must not link into the gitignored scratchpad](setup.md) (search: "## Footgun: release docs lag the version literals; committed docs must not link into the gitignored scratchpad") - `scripts/bump-version.sh` updates the four in-tree version literals plus `package.json`/lock and…
- [`stats --check` only greps a semantic anchor when the path sits immediately before `(search: ...)`](setup.md) (search: "## Footgun: `stats --check` only greps a semantic anchor when the path sits immediately before `(search: ...)`") - hallucination-risk: high (`.goat-flow/skill-docs/skill-preamble.md` states that `stats --check`…
- [`goat-flow audit --check-content` reports framework dashboard views as project drift; the fix it suggests is a false claim](setup.md) (search: "## Footgun: `goat-flow audit --check-content` reports framework dashboard views as project drift; the fix it suggests is a false claim") - hallucination-risk: high (the warning names `.goat-flow/code-map.md` as the path and supplies a…
- [`Confidence` vocabulary shadows the pre-ADR-009 severity vocabulary](severity.md) (search: "## Footgun: `Confidence` vocabulary shadows the pre-ADR-009 severity vocabulary") - hallucination-risk: high (an agent sweeping for "stale severity names" after ADR-009 will see…
- [Dashboard `fail-on` fallback must track the CLI default, not the previous one](severity.md) (search: "## Footgun: Dashboard `fail-on` fallback must track the CLI default, not the previous one") - hallucination-risk: medium (the migration touched the *parser* and the *CLI default* but not the…
