---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- ["dogfood must be grade A" tempts you to mute a tripping rule with a per-repo threshold override](calibration.md) (search: '## Footgun: "dogfood must be grade A" tempts you to mute a tripping rule with a per-repo threshold override') - The project invariant is that `go run ./cmd/gruff-go analyse .` returns grade A on `main`.
- [the scan-test-repos corpus is gitignored, so a root-relative scan finds nothing](calibration.md) (search: "## Footgun: the scan-test-repos corpus is gitignored, so a root-relative scan finds nothing") - The precision corpus lives under `.goat-flow/scratchpad/scan-test-repos/`, which is gitignored in the gruff-go repo (search `.goat-flow/scratchpad/.gitignore`).
- [finding filters do not isolate rule execution or scoring](calibration.md) (search: "## Footgun: finding filters do not isolate rule execution or scoring") - A probe run with `--include-rules naming.acronym-case` can render an empty `findings` array while the summary, composite, and exit code still reflect a hidden finding from another rule.
- [the agent hook re-implements `internal/diff` changed-region logic in bash — the two drift](hooks.md) (search: "## Footgun: the agent hook re-implements `internal/diff` changed-region logic in bash — the two drift") - hallucination-risk: high (both layers "do changed-region scoping", so a fix to the Go core reads as done even though the bash hook silently kept the old behaviour)
- [the hook is advisory exit 0 with in-band skip/ignore reporting — do not make it exit 2](hooks.md) (search: "## Footgun: the hook is advisory exit 0 with in-band skip/ignore reporting — do not make it exit 2") - `analyse` exits 2 when every explicit input is skipped before parsing: `ReportAllSkippedInputs` adds an error diagnostic and `ResolveExitCode` (`internal/analysis/report.go`, search: `func…
- [Workflow action ref safety is shape-based, not tag-aware](security-rules.md) (search: "## Footgun: Workflow action ref safety is shape-based, not tag-aware") - `security.github-actions-unpinned-action` is a parser-only text rule.
- [Template XSS must classify Execute receivers, not file imports](security-rules.md) (search: "## Footgun: Template XSS must classify Execute receivers, not file imports") - Importing `html/template` in a file does not make `text/template` auto-escaped.
- [secret-pattern precision guards are Go-only; config files and test fixtures both bite](security-rules.md) (search: "## Footgun: secret-pattern precision guards are Go-only; config files and test fixtures both bite") - The generic `sensitive-data.secret-pattern` rule (`internal/rule/builtin.go`, search: `func (r SensitiveDataRule) AnalyzeUnit`) is `SeverityError`, so a false positive fails the grade and blocks a…
- [`allowlists.acceptedAbbreviations` has different rule-consumer semantics than sibling gruff ports](setup.md) (search: "## Footgun: `allowlists.acceptedAbbreviations` has different rule-consumer semantics than sibling gruff ports") - hallucination-risk: high (the shared key name across gruff-go / gruff-rs / gruff-ts / gruff-py / gruff-php invites assuming shared semantics; the rules that consume the list are different in each…
- [`allowlists.secretPreviews` gates the preview field only - it does not suppress sensitive-data findings](setup.md) (search: "## Footgun: `allowlists.secretPreviews` gates the preview field only - it does not suppress sensitive-data findings") - hallucination-risk: medium (the field name and sibling configuration invite an incorrect mental model)
- [`gruff-go init --reset` wipes hand-tuned `.gruff-go.yaml` policy](setup.md) (search: "## Footgun: `gruff-go init --reset` wipes hand-tuned `.gruff-go.yaml` policy") - hallucination-risk: low (this is a behaviour to remember, not a fact to fabricate)
- [`npm test` exists but is a failing placeholder](setup.md) (search: "## Footgun: `npm test` exists but is a failing placeholder") - hallucination-risk: high
- [Scanner CLI exists, but published operational integration is still narrow](setup.md) (search: "## Footgun: Scanner CLI exists, but published operational integration is still narrow") - hallucination-risk: high
- [release docs lag the version literals; committed docs must not link into the gitignored scratchpad](setup.md) (search: "## Footgun: release docs lag the version literals; committed docs must not link into the gitignored scratchpad") - `scripts/bump-version.sh` updates the four in-tree version literals plus `package.json`/lock and the CLI goldens, but deliberately leaves `CHANGELOG.md`, `README.md`, and `docs/` (search the script…
- [`Confidence` vocabulary shadows the pre-ADR-009 severity vocabulary](severity.md) (search: "## Footgun: `Confidence` vocabulary shadows the pre-ADR-009 severity vocabulary") - hallucination-risk: high (an agent sweeping for "stale severity names" after ADR-009 will see `"high"`, `"medium"`, and `"low"` in many golden files and source files, and may try to "fix" them — but…
- [Dashboard `fail-on` fallback must track the CLI default, not the previous one](severity.md) (search: "## Footgun: Dashboard `fail-on` fallback must track the CLI default, not the previous one") - hallucination-risk: medium (the migration touched the *parser* and the *CLI default* but not the dashboard fallback; the dashboard's fallback constant looks correct in isolation and only diverges…
