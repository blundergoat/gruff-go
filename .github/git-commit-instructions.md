# Git Commit Instructions

Use concise conventional commits where practical, such as `chore: set up goat flow` or `docs: update project memory`.

Keep commits scoped to the task. Do not commit `node_modules/`, local IDE metadata, Claude local settings, session scratch files, or unrelated user changes.

No additional project-specific commit constraints are currently documented.
